# _src

`/sf-memory` が収集・保存する中間データ。sf-doc / sf-design のInputとなる。

| ファイル | 生成元 | 用途 |
|---|---|---|
| `feature_groups.yml` | sf-memory カテゴリ5 | 業務機能グループ定義（詳細設計の生成単位） |

**注意**: このフォルダのファイルは手動編集可能。`/sf-memory` 再実行時は差分更新（手動修正は保持される）。
